let greet = "Hello!\nHowzit!\nBonjour!";
console.log(greet);
let oneLineDemos =
  "one \
long \
line";
console.log(oneLineDemos);

/*escape sequence is useful when you need to
 *include an apostrophe in a string literal that is contained *within single quotes
 
instead of using it to
mark the end of the string, you use it as an apostrophe:
*/
const quotes = "I can't come today";
console.log(quotes);
