//if the given maximum string length is less than or equal to three,
function truncateString() {}
truncateString("Tommy goes to work", 8);

//slice includes the first index and returns everything before the last index;
//example:
let name = "Matshidiso";
let sliced = name.slice(2, 8);
console.log(sliced);
let colors = ["red", "green", "yellow"];
let newColors = colors.slice(2);
console.log(newColors);
